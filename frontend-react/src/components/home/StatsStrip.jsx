import React, { useState } from 'react';
import { CountUp, useInView } from '../ui/Motion';
const stats=[
  [91.07,2,'%', 'Holdout accuracy','51 of 56 unseen test records were correctly classified.'],[84.38,2,'%', 'Balanced accuracy','Balances evaluation across the imbalanced holdout classes.'],[.9392,4,'','PR-AUC','Measures positive-class ranking performance on holdout data.'],[25,0,'-Fold','Cross-validation','Repeated stratified folds were used during model evaluation.'],[15,0,'','Model features','Two demographics and thirteen survey indicators form the feature vector.'],
];
export default function StatsStrip(){const [ref,visible]=useInView();const [active,setActive]=useState(0);return <section ref={ref} aria-label="Verified model performance" className="border-y border-pearl-300 bg-white py-9 sm:py-11"><div className="mx-auto max-w-7xl px-6 sm:px-8 lg:px-12"><div className="mb-5 flex flex-wrap items-center gap-2 text-caption uppercase tracking-[.14em] text-pearl-600"><span className="h-px w-6 bg-clinical-500"/>Model evaluation <strong className="font-mono text-ink-900">≠</strong> individual patient probability</div><div className="grid grid-cols-2 border border-pearl-300 md:grid-cols-5">{stats.map(([value,decimals,suffix,label,detail],i)=><button type="button" key={label} onClick={()=>setActive(i)} onMouseEnter={()=>setActive(i)} onFocus={()=>setActive(i)} className={`metric-datum ${active===i?'is-active':''} ${visible?'motion-reveal is-visible':'motion-reveal'}`} style={{transitionDelay:`${i*60}ms`}} aria-pressed={active===i}><span className="font-mono text-2xl font-extrabold tracking-tight sm:text-3xl"><CountUp value={value} decimals={decimals} suffix={suffix}/></span><strong>{label}</strong><span className="metric-more">{active===i?detail:'Select for method note'}</span></button>)}</div><div className="mt-3 border-l-2 border-clinical-500 bg-clinical-50 px-4 py-3 text-caption text-ink-800"><strong>Selected datum:</strong> {stats[active][4]}</div></div></section>}
